#include "days.h"

constexpr const char *const INPUT_FILE = "../../input.txt";
constexpr const char *const OUTPUT_FILE = "../../output.txt";

void configIO() {
    freopen(INPUT_FILE, "r", stdin);
    freopen(OUTPUT_FILE, "w", stdout);
}

int main() {
    configIO();
}